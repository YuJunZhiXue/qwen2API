Nothing should be ignored in this case, as the changes only include documentation (MANUAL_INSTALACAO.md) and a script file (install.sh), which are source/config files without any build artifacts, dependencies, or temporary files present in the change list. Therefore, the output is empty.